# Architecture

> Auto-generated by /map on 2026-03-15

## Overview

A React-based web application (Ganttagram) for project management using Gantt charts, integrated with Firebase for data storage and authentication. It features a private dashboard, project viewing, and comprehensive task management.

```
┌─────────────────────────────────────────┐
│        App.jsx (React Router)           │
├─────────────────────────────────────────┤
│    Features (Auth, Dashboard, Gantt)    │
├─────────────────────────────────────────┤
│     Services (Firebase Firestore)       │
└─────────────────────────────────────────┘
```

## Components

### Auth
- **Purpose:** User authentication (login, registration, password reset) and protected route management.
- **Location:** `src/features/auth`
- **Dependencies:** Firebase Auth, React Context

### Dashboard
- **Purpose:** Main user interface post-login, listing projects or high-level status.
- **Location:** `src/features/dashboard`
- **Dependencies:** Projects feature

### Projects & Milestones
- **Purpose:** Management and viewing of projects and their respective milestones.
- **Location:** `src/features/projects`, `src/features/milestones`

### Tasks & Gantt
- **Purpose:** Creation, scheduling, and visualization of tasks using Gantt charts.
- **Location:** `src/features/tasks`, `src/features/gantt`
- **Dependencies:** `gantt-task-react`

### Resources & Dependencies
- **Purpose:** Handling task assignments (resources) and task relationships (dependencies).
- **Location:** `src/features/resources`, `src/features/dependencies`

## Data Flow

1. User interacts with UI components in `src/features/*`.
2. State and Context (`src/shared/context`) manage local state (ThemeProvider, AuthProvider).
3. Reads/Writes are handled via Firebase services (`src/services/firebase/config.js`).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Firebase | Database/Auth | Data persistence and user authentication |

## Technical Debt

- [ ] Review TODO at `src/features/tasks/utils/hierarchy.js:90` about recalculating ancestors.

## Conventions

**Naming:** PascalCase for React components (`DashboardPage.jsx`), camelCase for utilities/functions (`hierarchy.js`).
**Structure:** Feature-based directory organization (`src/features/*`), shared logic in `src/shared`.
**Testing:** Vitest setup with React Testing Library.
