# Architecture

> Auto-generated by /map on 2026-03-09

## Overview

Este es un sistema de gestión de proyectos tipo Ganttagram. Permite organizar tareas, hitos y recursos en forma de diagramas de Gantt interactivos. Es una aplicación frontend en React, que se apoya en Firebase Firestore para almacenamiento y autenticación, y está preparada para empaquetarse con Tauri.

```text
┌─────────────────────────────────────────┐
│           [src/main.jsx]                │
├─────────────────────────────────────────┤
│     [Features (projects, tasks)]        │
├─────────────────────────────────────────┤
│         [Services (Firebase)]           │
└─────────────────────────────────────────┘
```

## Components

### Features Auth
- **Purpose:** Manejo de la autenticación de usuarios.
- **Location:** `src/features/auth/`
- **Dependencies:** Firebase Auth.

### Features Projects
- **Purpose:** Gestión de proyectos, listas de proyectos y Kanban Boards.
- **Location:** `src/features/projects/`
- **Dependencies:** Firestore (`useProjects`, `useMilestones`, etc.), React.

### Features Tasks
- **Purpose:** Gestión y renderizado de tareas individuales.
- **Location:** `src/features/tasks/`
- **Dependencies:** Firestore (`useTasks`), hooks de dependencias.

### Features Gantt
- **Purpose:** Visualización principal de los proyectos usando un diagrama de Gantt interactivo.
- **Location:** `src/features/gantt/`
- **Dependencies:** `gantt-task-react` y utilidades de fechas.

## Data Flow

1. La interfaz manda una acción (crear tarea).
2. Los hooks (`useTasks`, `useProjects`) captura la interacción.
3. El estado del backend cambia usando Firebase (`services/firebase/config.js`).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Firebase | BaaS | Autenticación, Base de datos (Firestore) principal. |
| Tauri | Desktop | Permite construir la aplicación web como una aplicación de escritorio. |

## Technical Debt

- [ ] Mejorar convenciones de código al idioma de preferencia (e.g. status `todo` se puede confundir con tarea).
- [ ] Centralizar manejo de errores y logging de Firestore. No se detectaron boundaries o interceptores globales en este análisis básico.
- [ ] Faltan validaciones estrictas tipo Zod (recomendado basado en el estilo de codificación del framework).

## Conventions

**Naming:** PascalCase para componentes, camelCase para hooks y utils.
**Structure:** Arquitectura modular enfocada en "Features".
**Testing:** Vitest con React Testing Library (ej: `useProjects.test.js`).
