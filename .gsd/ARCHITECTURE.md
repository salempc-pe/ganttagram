# Architecture

> Auto-generated by /map on 2026-03-05

## Overview

Ganttagram is a project management and Gantt chart web application built with React, Vite, and Firebase. It provides features for managing projects, tasks, milestones, and resources with a focus on visual scheduling via Gantt charts.

```
┌─────────────────────────────────────────┐
│              [Entry Point]              │
│          src/main.jsx & App.jsx         │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│    src/features (auth, tasks, etc.)     │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│    src/services/firebase/config.js      │
└─────────────────────────────────────────┘
```

## Components

### Features
- **Purpose:** Domain-specific logic and UI components separated by bounded contexts (auth, dashboard, gantt, projects, tasks, resources).
- **Location:** `src/features/*`
- **Dependencies:** React hooks, Firebase Firestore/Auth.

### Shared
- **Purpose:** Common UI components and utilities across the application.
- **Location:** `src/shared/*`

### Services
- **Purpose:** Infrastructure and external integrations (Firebase).
- **Location:** `src/services/*`

## Data Flow

1. User interacts with UI components in `src/features/*`.
2. Component triggers custom hooks (e.g., `useTasks`, `useProjects`).
3. Hooks interact with Firebase Firestore/Auth via `src/services/firebase/config.js`.
4. State updates trigger UI re-renders through React Context or component state.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Firebase | BaaS | Authentication and Firestore database for state persistence. |

## Technical Debt

- [ ] Firebase timeout fallback logic in auth needs review.
- [ ] No explicit TODOs identified, but codebase may lack comprehensive unit tests based on current findings.

## Conventions

**Naming:** PascalCase for React components, camelCase for hooks and utilities. Feature-based structure (`src/features/...`).
**Structure:** Feature-driven architecture, separating layers into UI, hooks, and services.
**Testing:** Exists `vitest` in devDependencies but tests structure might need more coverage given lack of obvious `__tests__` directories inside features.
